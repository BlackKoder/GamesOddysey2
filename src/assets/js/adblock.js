/*const detect = document.querySelector("#detect");
const wrapper = document.querySelector(".wrapper-ad");
const avisoAdblock = document.querySelector("#fondo-aviso-adblock");

let adClasses = ["ad", "ads", "adsbox", "ad-placement", "doubleclick", "ad-placeholder", "ad-badge", "BannerAd"];
  for(let item of adClasses){
    detect.classList.add(item);
}

let getProperty = window.getComputedStyle(detect).getPropertyValue("display");

if (!wrapper.classList.contains("show")) {
getProperty == "none" ? wrapper.classList.remove("show") : wrapper.classList.add("show");
avisoAdblock == "none" ? avisoAdblock.classList.remove("activo") : avisoAdblock.classList.add("activo");
console.log("Estoy funcionando " + wrapper.getAttribute("display"));

}*/
/*if (getProperty == "none") {
	wrapper.classList.add("show");	
}else{
	wrapper.classList.add("show");
}*/